quarkus:
  messaging:
    nats:
      key-value-stores:
        credentials:
          storage-type: File
          bucket-name: "${capacnan.credentials.kv.bucket}"
      streams:
        credentials:
          name: "${capacnan.credentials.stream}"
          configuration:
            subjects: ${capacnan.credentials.subjects}
            storage-type: File
